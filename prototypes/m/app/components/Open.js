import React from "react"
let ReactPropTypes = React.PropTypes

let Open = React.createClass({

  propTypes: {
    params: ReactPropTypes.object
  },

  render: function() {

    return (
      <div className="opan">
      </div>
    )
  }
})

module.exports = Open
